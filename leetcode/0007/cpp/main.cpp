#include <cassert>
#include <iostream>
using namespace std;

class Solution {
public:
    int reverse(int x) {

    }
};

int main()
{
    Solution s;

    //assert(s.reverse(123) == 321);
    //assert(s.reverse(-123) == -321);
    //assert(s.reverse(120) == 21);

    return 0;
}
